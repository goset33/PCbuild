using UnityEngine;
using UnityEngine.Events;

public class BuildConnected : MonoBehaviour
{
    public int indexConnect;
    public UnityEvent onConnect;
    // Start is called before the first frame update
}
